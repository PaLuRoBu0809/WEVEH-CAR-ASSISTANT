server:
  port: ${SERVER_PORT:8080}

weveh:
  llm:
    base-url: ${LLM_BASE_URL:https://openrouter.ai/api/v1}
    api-key: ${LLM_API_KEY}
    modelo: ${LLM_MODELO:openai/gpt-4o}
    temperatura: ${LLM_TEMPERATURA:0.1}
    max-tokens: ${LLM_MAX_TOKENS:1800}
    timeout-ms: ${LLM_TIMEOUT_MS:15000}
